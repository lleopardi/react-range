import * as React from "react";

const Home = () => {
    return (
        <div>
            Demo de input range(Sin el input), solo consta de divs y Drag and Drop
        </div>
    );
}

export default Home;
