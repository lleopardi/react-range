# This project is a demo of reactJS
## Follow the steps
1. Run `npm i`
2. Run `npm start` for start the project
3. Run `npm run test` for execute the unit tests
4. Run `npm run coverage` if you can see the code coverage